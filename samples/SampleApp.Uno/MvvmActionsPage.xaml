<Page x:Class="SampleApp.Uno.MvvmActionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:kumiko="using:KumikoUI.Uno"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="MVVM + Per-Column EditTriggers + Action Buttons"
                   FontSize="18"
                   FontWeight="Bold"
                   Padding="16,12,16,2"
                   Grid.Row="0" />

        <!-- Per-column trigger legend -->
        <StackPanel Grid.Row="1" Padding="16,0,16,8" Spacing="2">
            <TextBlock FontSize="12" Foreground="Gray" TextWrapping="Wrap"
                       Text="Name: single-tap / F2 / type    Department: double-tap or F2    Level: double-tap only" />
            <TextBlock FontSize="12" Foreground="Gray" TextWrapping="Wrap"
                       Text="Salary: F2 only (most protected)    Actions: single-tap (Details / Delete commands bound to the VM)" />
        </StackPanel>

        <!--
            Columns are built in code-behind because the Actions column's ActionButtonsCellRenderer
            wires its buttons to the ViewModel's ICommands (Details / Delete). x:Name lets the
            code-behind populate Columns and ItemsSource after the VM is constructed.
        -->
        <kumiko:DataGridView x:Name="actionsGrid"
                             Grid.Row="2"
                             EditTriggers="DoubleTap,F2Key,Typing"
                             RowHeight="44"
                             HeaderHeight="40"
                             GridDescription="MVVM employee grid with action buttons"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch" />
    </Grid>

</Page>
